const fs = require("fs"); let content = fs.readFileSync("components/calculator/project-build/CompareModal.tsx", "utf-8"); content = content.replace(//g, "&bull;"); content = content.replace(/??/g, `<Info className="w-5 h-5 text-primary shrink-0" />`); content = content.replace(/??/g, `<Lightbulb className="w-5 h-5 text-primary shrink-0" />`); if(!content.includes("Lightbulb")) { content = content.replace(`import { X, Download } from "lucide-react";`, `import { X, Download, Lightbulb, Info } from "lucide-react";`); } fs.writeFileSync("components/calculator/project-build/CompareModal.tsx", content); console.log("Done");
